{
  "name": "x-charts",
  "version": "0.0.0",
  "main": "",
  "exports": "",
  "type": "module",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "flow": "flow ./src/",
    "flow-build-full": "rm -rf ./dist/modules/* && flow ./src/ && flow-remove-types -d ./dist/modules/ -i '(^src\\/test-unit\\/|([^./]+\\.|/)types\\.js)' ./src/",
    "flow-build": "flow-remove-types",
    "flow-build-test": "flow-remove-types -d ./test/unit-tmp/",
    "tape": "tape",
    "nodemon": "nodemon --watch ./test/server/ -- ./cmd.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dmitry Koroliov <d.koroliov@gmail.com>",
  "license": "MIT",
  "private": true,
  "description": "",
  "devDependencies": {
    "flow-bin": "^0.259.1",
    "flow-remove-types": "^2.259.1",
    "livereload": "^0.9.3",
    "nodemon": "^3.1.9",
    "tape": "^5.9.0"
  }
}
